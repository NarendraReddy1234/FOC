#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
	int a,b,c;
	float s,d,area;
	printf("enter three sides");
	scanf("%d%d%d",&a,&b,&c);
	s=(s*(s-a)*(s-b)*(s-c));
	area=sqrt(d);
	printf(" area of triangle=%of sq units\n",area);
}
